# Source Code

- `open_challenge/`: Open Challenge entry point and challenge-specific logic
- `obstacle_challenge/`: Obstacle Challenge entry point and challenge-specific logic

Document the language, controller, dependencies, build command, upload procedure, and the purpose of every module. Important logic should also contain useful comments in the source files.

